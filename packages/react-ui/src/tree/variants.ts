// Tree styling lives in ../html/tree — re-exported here so consumers can
// compose the same variants without reaching into html/.
export { treeRootVariants, treeChildWrapperVariants, treeVerticalLineVariants } from "../html/tree";
