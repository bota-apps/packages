import { cva } from "class-variance-authority";

export const detailFieldIconVariants = cva("text-muted-foreground mt-0.5 [&>svg]:h-5 [&>svg]:w-5");

export const infoRowVariants = cva("text-sm text-muted-foreground");
