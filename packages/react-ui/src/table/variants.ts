// Table styling lives in ../html/table — re-exported here so consumers can
// compose the same variants without reaching into html/.
export { trVariants, thVariants, tdVariants } from "../html/table";
