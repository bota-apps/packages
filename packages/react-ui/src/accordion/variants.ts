// Accordion styling lives in ../html/accordion.tsx (the html layer is the
// source of truth); this module re-exports the cvas for consumers.
export {
  accordionRootVariants,
  accordionItemVariants,
  accordionTriggerVariants,
  accordionContentVariants,
} from "../html/accordion";
