// The layout CVA lives in html/layout — the styling source of truth.
export {
  boxVariants,
  stackVariants,
  inlineVariants,
  gridVariants,
  centerVariants,
  containerVariants,
} from "../html/layout";
