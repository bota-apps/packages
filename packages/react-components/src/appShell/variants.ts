// AppShell composes AppShellLayout with slots and stays free of className —
// the chrome's styling (and its cva) lives in appShellLayout, the styling
// source of truth.
export { appShellLayoutVariants } from "../appShellLayout/variants";
