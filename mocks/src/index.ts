// @bota-apps/mocks — in-memory doubles for tests and stories, one directory per
// module they stand in for. Every mock returns the REAL client type, so it can
// be handed to anything that accepts the production client.
export * from "./auth";
export * from "./graphql";
