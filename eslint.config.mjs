// The monorepo's own lint config is the shared package — single source of truth,
// dogfooded here and shipped to generated apps as @bota-apps/eslint-config.
import bota from "@bota-apps/eslint-config";

export default bota;
