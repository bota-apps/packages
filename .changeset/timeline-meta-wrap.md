---
"@bota-apps/react-ui": patch
---

`TimelineItem`: the title/meta row now wraps in narrow containers — the timestamp drops under the title instead of pushing the row wider than the timeline's container.
