---
"@bota-apps/export": patch
---

Docs & metadata: add package keywords, a structured author field, and an expanded README, and correct the `package.json` description (the barrel dispatcher is `exportTable()`, not `exportReport()`). No runtime or API changes.
