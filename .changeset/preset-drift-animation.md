---
"@bota-apps/tailwind-preset": minor
---

New `drift` keyframes with `animate-drift` / `animate-drift-reverse` utilities — a slow ambient float for decorative background layers (used by the Hero aurora treatment). Transform-only so it stays on the compositor; pair with `motion-reduce:animate-none` at the call site like every decorative loop.
